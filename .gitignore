node_modules/
dist/
.DS_Store
coverage/

# Local secrets and machine-specific environment files
.env
.env.*
!.env.example

# BASE759 is intentionally versioned as a read-only classic reference corpus.

# Bun is the canonical package manager for this project.
package-lock.json
yarn.lock
pnpm-lock.yaml
